# docker-compose.yml
version: '3.9'
services:
  db:
    image: postgres:16
    container_name: elevate-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: elevate_dev
    ports:
      - "5432:5432"